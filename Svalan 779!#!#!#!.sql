-- create table
CREATE TABLE cities
(
  	id INT PRIMARY KEY NOT NULL auto_increment,
	dag VARCHAR(255),
	mat VARCHAR(255),
	vegetarisk VARCHAR(255),
	glasyr VARCHAR(255)
);

-- Insert data into our table
INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');

INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');

INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');

INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');

INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');

INSERT INTO cities(dag, mat, vegetarisk, glasyr)
VALUE ('Måndag', 'biff', ärta, 'chokolate');